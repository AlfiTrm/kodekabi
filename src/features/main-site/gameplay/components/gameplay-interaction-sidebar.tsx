import type { ReactNode } from "react";

export function GameplayInteractionSidebar({ panel, onPanelChange, children, submitMessage, canDecide, pending, decisionLabel, onDecision }: { panel: "questions" | "interrogation"; onPanelChange: (panel: "questions" | "interrogation") => void; children: ReactNode; submitMessage: string | null; canDecide: boolean; pending: boolean; decisionLabel: string; onDecision: () => void }) {
  return <aside className="flex min-h-[calc(100vh-4rem)] flex-col border-l border-border bg-background p-4 sm:p-6 lg:fixed lg:bottom-0 lg:right-0 lg:top-16 lg:z-20 lg:w-[360px]"><div className="shrink-0 grid grid-cols-2 gap-1 rounded-full bg-surface-muted p-1"><button type="button" onClick={() => onPanelChange("questions")} className={`h-9 rounded-full text-[10px] font-semibold ${panel === "questions" ? "bg-white text-button-ink" : "text-foreground/50"}`}>Pertanyaan</button><button type="button" onClick={() => onPanelChange("interrogation")} className={`h-9 rounded-full text-[10px] font-semibold ${panel === "interrogation" ? "bg-white text-button-ink" : "text-foreground/50"}`}>Interogasi</button></div><div className="min-h-0 flex-1 overflow-y-auto">{children}{submitMessage ? <p className="mt-4 rounded-xl border border-red/30 bg-red/8 px-3 py-2 text-[10px] text-red">{submitMessage}</p> : null}</div><div className="shrink-0 pt-5"><button type="button" onClick={onDecision} disabled={!canDecide || pending} className={`h-11 w-full shrink-0 rounded-full text-xs font-bold transition-colors disabled:cursor-not-allowed ${canDecide ? "bg-white text-button-ink hover:bg-foreground" : "bg-surface-muted text-foreground/35"}`}>{decisionLabel}</button></div></aside>;
}
